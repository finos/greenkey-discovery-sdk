---
entities:
  room_number:
    - "@num{1,4}"
intents:
  digit:
    domain: digit
    examples:
      - "please select @room_number to speak to the operator"
      - "to change your order press @room_number"
      - "if this is an emergency dial @room_number"
      - "for animal control services press @room_number"
